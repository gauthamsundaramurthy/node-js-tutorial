
module.exports.counterFunction = function (arr) {
    return 'There are ' + arr.length + ' items'
}

module.exports.adderFunction = function (a,b) {
    return `The sum of two numbers is ${a+b} ` 
}

module.exports.pi = 3.14
